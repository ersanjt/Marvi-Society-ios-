# Copy to keystore.properties (gitignored). Used for release signing.
# Generate keystore: bash ../../scripts/google-play/generate-keystore.sh

storeFile=marvi-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=marvi
keyPassword=YOUR_KEY_PASSWORD
